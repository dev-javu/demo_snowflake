name: Deploy

on:
  push:
    branches: [dev, production]
    paths:
      - 'migrations/**'
      - 'terraform/**'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_base.yml
    with:
      dry_run: false
    secrets: inherit
